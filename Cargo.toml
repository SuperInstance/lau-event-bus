[package]
name = "lau-event-bus"
version = "0.1.0"
edition = "2021"
description = "Pub/sub event bus — the nervous system connecting all PLATO crates"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
